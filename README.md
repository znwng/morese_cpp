# Morese

A morse code encoder and decoder that runs entirely on binary tree.
The binary tree is a replica of the morse tree.

Can encode and decode command line inputs and files.

Built it cuz bored.

